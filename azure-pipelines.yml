
# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger: none
pr: none

resources:
- repo: self

variables:
  
  acrName: aksdemoregistrygreen231
  imageName: user-service
  tag: $(Build.BuildId)
  resourceGroup: project_rg
  aksCluster: aks-demo-cluster

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Docker@2
      displayName: Build an image
      inputs:
        command: build
        dockerfile: '$(Build.SourcesDirectory)/services/user-service/Dockerfile'
        tags: |
          $(tag)


pool:
  vmImage: 'ubuntu-latest'




stages:

# ✅ Build & Push Docker Image
- stage: Build
  jobs:
    - job: BuildImage
      steps:

        - task: Docker@2
          displayName: Build and Push Image
          inputs:
            containerRegistry: 'ACR-SERVICE-CONNECTION'
            repository: $(imageName)
            command: buildAndPush
            Dockerfile: services/user-service/Dockerfile
            tags: |
              $(tag)

# ✅ Deploy to AKS
- stage: Deploy
  dependsOn: Build
  jobs:
    - job: DeployToAKS
      steps:

        - task: Kubernetes@1
          inputs:
            connectionType: 'Azure Resource Manager'
            azureSubscriptionEndpoint: 'AZURE-SERVICE-CONNECTION'
            azureResourceGroup: $(resourceGroup)
            kubernetesCluster: $(aksCluster)
            command: login

        - script: |
            helm upgrade --install user-service ./helm/user-service \
              --set image.repository=$(acrName).azurecr.io/user-service \
              --set image.tag=$(tag)
          displayName: Deploy via Helm

